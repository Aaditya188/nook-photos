# Expo HAS CHANGED

This app is pinned to **Expo SDK 54** (see `package.json`) because that is what ships in Expo Go on
the App Store and Play Store. Read the exact versioned docs at
https://docs.expo.dev/versions/v54.0.0/ before writing any code — do NOT follow newer-SDK docs.
